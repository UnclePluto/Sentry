import type { Metadata } from 'next';
export const metadata: Metadata = { title: '管理员登录 · SENTRY' };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
