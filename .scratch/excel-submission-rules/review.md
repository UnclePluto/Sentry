# Excel 提交规则重构：审查与验证

日期：2026-09-16

比较基准：本轮实现开始前保存的只读源代码快照。当前项目不是 Git 仓库，因此没有可解析的分支、提交列表或可执行的代码提交；没有擅自初始化仓库。

## Standards

独立规范审查未发现违反项目明确标准的阻塞项。新文案区分检测批次、试剂与上传批次，历史管理按整批作废描述，验收沿用真实 HTTP 接口。

保留两项非阻塞维护建议：

1. 当前迁移从建表文本中提取目标提交表定义。未来增加新版本迁移时，应冻结各版本的目标结构，避免格式变化影响旧版本升级。
2. 上传、统计、演示及界面中存在相同的直辖市代码判断。目前规则一致，后续扩展行政区模型时可集中维护。

## Spec

独立规格审查发现一项问题：富文本单元格未清理首尾空白，可能绕过重复校验。

已在真实 HTTP 测试边界复现失败，再统一富文本与文本对象的规范化；回归测试覆盖富文本重复标识、带空白的 N 和横线对照。修复后审查者复核关闭该问题，当前无未解决的规格发现。

## 验证

- 完整测试套件：13 项通过，0 项失败，0 项跳过，包含用户原始样表。
- 类型检查通过，生产构建通过；构建保留已有大文件分包提示。
- 新增及重写的解析、存储、API、演示、上传界面、类型与 HTTP 测试通过定向 lint，无错误；React Compiler 的 effect 警告仍存在。
- 全项目 lint 未通过：已有登录页自动聚焦、内部链接、旧地图 Hook 依赖、废弃事件类型等问题未纳入本次业务改动，不将其宣称为通过。
- 旧数据库升级先创建受限权限 SQLite 快照并检查完整性，再迁移；测试验证重启不重复升级，原账号与历史审计保留。
- 真实样表新口径为：294 条有效病原体结果、199 个有效试剂、199 个阳性试剂、84 行剔除。原表中的 N 阴性也被剔除，因此阳性率为 100%。
- 未进行浏览器视觉验收。

## 本机验收

服务已重启：展示入口 3000、管理入口 3002；页面、登录保护、地图数据及静态资源冒烟检查通过。真实本机数据库已升级到版本 2，96 条演示提交保留，完整性为 ok、外键错误为 0；升级前快照权限为 0600。
