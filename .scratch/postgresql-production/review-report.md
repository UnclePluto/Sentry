# PostgreSQL 实现双轴审查

日期：2026-09-16。用户确认基线：`6fb14c9`。主审范围为 `git diff 6fb14c9...HEAD`，后续修复及收尾差异另行复核。来源规格为同目录 `spec.md` 与 11 张任务票。两路独立子代理只读审查。

## Standards

初审未发现硬性规范违规。1 项判断性代码气味：维护模式检查分散在多个写入入口，可能发生遗漏或锁顺序分叉。已抽取 `server/write-gate.mjs`，统一写入门禁、身份复验及锁顺序。复审确认该项解决，无新增发现。

## Spec

初审发现 3 项实现问题，均已修复并复审：

1. P1：读取慢请求正文期间账号被停用或改密，仍可能沿用请求开始时的身份。写事务现在再次校验账号、会话与有效期；真实 HTTP 竞争回归通过。
2. P2：不同批次同时作废时共享锁升级可能死锁。所有写事务统一先锁数据版本行，再锁账号／会话、任务及批次；并发作废回归通过。
3. P2：最后一次手动重试失败后仍显示可重试，但接口拒绝。终止状态现在明确 `retryable=false` 并说明原因；第 5 次故障回归通过。

浏览器收尾发现同一任务确认后轮询未可靠重启，已使用显式版本重启监听并走通实际表单完整流程。旧预览过期任务及非 UUID 标识查询也经过专项测试与只读复核，无新增阻断。新增备份演练以实际脚本结果作为证据，不冒充代理已经复核其全部代码。

09 的真实 OSS 连通、授权异常及下载恢复尚未验证；11 未上线。这是尚未满足的交付门槛，不能用代码审查通过替代。生产仍运行旧 SQLite 镜像。

Standards：初审 1 项气味、0 项硬性违规，已解决；Spec：初审 3 项实现问题，已解决，仍保留 09／11 的外部验收门槛。
