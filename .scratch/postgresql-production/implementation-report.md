# 实施与验收记录

日期：2026-09-16。审查基线：`6fb14c9`。未切换生产数据库，未做容量或并发压测。

## 已实现

PostgreSQL 17 单一运行数据库；异步连接池和事务；保留账号与会话；独立持久化任务工作进程；Sheet1 三万非空行限制；私有原文件和过期暂存；幂等发布、整批作废及分页历史；持久化批次汇总和一致性重建；大盘 20 秒刷新；SQLite 一致性快照迁移；容器资源与日志限制；pgBackRest 基础备份及 WAL；维护开关和回退手册。

## 已取得的证据

- 全量测试 17 项通过、0 跳过（含用户真实 Excel）。之后补充旧租约执行者文件删除竞争回归，单文件复验另行记录。
- 真实样表：有效结果 294 行、有效试剂 199、剔除对照 84；重复上传累计、作废扣除正确。
- 30,000 非空行接受，30,001 拒绝；含对照行、不含表头。
- 真实 PostgreSQL + API + worker：账号权限、跨重启任务、并发确认幂等、取消、原文件丢失、7 天过期、24 小时清理、维护保护、汇总重建通过。
- 线上一致性快照隔离迁移：账号 1、批次 97、试剂 5,152、结果 41,216；46 组统计核对一致。快照及含账号数据报告只存私有 `data/`，不入版本库。后续增加逐报告日期核对，正式切换需再次取最新快照。
- 浏览器使用既有超管登录迁移后的 `/admin/upload` 成功；原待确认批次可查看；通过实际表单重新上传用户样表并正确显示上述汇总。
- PostgreSQL/备份镜像和应用生产镜像构建成功；独立 Compose 的 PG、应用、worker 均健康；PG 没有发布宿主机端口，迁移身份与业务身份分离。
- 独立容器真实业务 PITR：发布→完整备份→作废→WAL归档→恢复到作废前，核对已发布状态、账号、3 条结果、2 个试剂、1 个阳性；缺失仓库恢复明确失败。约 31.89 秒，仅本机小数据、本地仓库，**OSS 未验证**。
- 类型检查通过。新业务文件专项静态检查无错误；仓库全量 lint 尚有原有地图、登录页和浏览器脚本等问题，不宣称全量 lint 通过。

## 未完成及外部依赖

- 09：ECS IMDSv2 角色查询返回 HTTP 404，未取得 OSS 授权。真实 OSS 连通性、授权失效/缺 WAL 场景、30 天保留链受控时间验证尚未全部完成；不能标记整票完成。
- 11：按已确认规格，必须等待真实 OSS 恢复及验收通过后才允许正式切换。当前线上仍运行原镜像和 SQLite，未改写在线库。
- RPO ≤15 分钟、RTO ≤1 小时、1,000 万条和 P95 ≤2 秒均为目标，当前没有生产实测证明。

操作说明见 `deploy/POSTGRESQL.md`。本报告后续补充审查和最终浏览器结果，不修改父规格。
