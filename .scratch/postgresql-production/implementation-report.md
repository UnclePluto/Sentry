# 实施与验收记录

日期：2026-09-16。审查基线：`6fb14c9`。已于北京时间 2026-09-16 16:25:51 完成生产切换；未做容量或并发压测。

## 已实现

PostgreSQL 17 单一运行数据库；异步连接池和事务；保留账号与会话；独立持久化任务工作进程；Sheet1 三万非空行限制；私有原文件和过期暂存；幂等发布、整批作废及分页历史；持久化批次汇总和一致性重建；大盘 20 秒刷新；SQLite 一致性快照迁移；容器资源与日志限制；pgBackRest 基础备份及 WAL；维护开关和回退手册。

## 已取得的证据

- 最终全量测试 25 项通过、0 失败、0 跳过（约 21.96 秒，含仅在本机使用的用户真实 Excel）；覆盖新增上传中断／写盘失败、登记失败清理和删除失败重启恢复。
- 真实样表：有效结果 294 行、有效试剂 199、剔除对照 84；重复上传累计、作废扣除正确。
- 30,000 非空行接受，30,001 拒绝；含对照行、不含表头。
- 真实 PostgreSQL + API + worker：账号权限、跨重启任务、并发确认幂等、取消、原文件丢失、7 天过期、24 小时清理、维护保护、汇总重建通过。
- 初次线上快照的本地隔离迁移：账号 1、批次 97、试剂 5,152、结果 41,216；46 组统计核对一致。正式切换重新冻结数据，最终 53 组核对一致。快照及含账号数据报告仅存私有目录，不入版本库。
- 浏览器使用既有超管登录迁移后的 `/admin/upload` 成功；原待确认批次可查看；通过实际表单重新上传用户样表并正确显示上述汇总。
- PostgreSQL/备份镜像和应用生产镜像构建成功；独立 Compose 的 PG、应用、worker 均健康；PG 没有发布宿主机端口，迁移身份与业务身份分离。
- 独立容器真实业务 PITR：发布→完整备份→作废→WAL归档→恢复到作废前，核对已发布状态、账号、3 条结果、2 个试剂、1 个阳性；缺失仓库恢复明确失败。最近完整演练约 48.18 秒，仅本机小数据、本地仓库；后续真实 OSS 验证见下方生产记录。恢复后使用原账号通过真实 HTTP 登录；缺失仓库及缺失 WAL 均明确失败。
- 类型检查通过。新业务文件专项静态检查无错误；仓库全量 lint 尚有原有地图、登录页和浏览器脚本等问题，不宣称全量 lint 通过。

## 验证边界

- 09 的 RAM 角色和 OSS 实连依赖已解决；11 已完成生产切换。11 张票均已完成。
- 备份目标按用户最新确认改为武汉 `floatnoise-wh`，见 ADR 0004；父规格保留原始讨论记录，不反向改写。
- RPO ≤15 分钟、RTO ≤1 小时、1,000 万条和 P95 ≤2 秒均为设计目标；小数据演练成功不能外推容量、高负载或完整灾难场景达标。

操作说明见 `deploy/POSTGRESQL.md`。不修改父规格。

## 收尾验收与审查

- 最终容器浏览器通过登录→实际表单上传→查看汇总→确认成功→历史记录→退出→重新登录。浏览器发现同一任务在确认后未可靠重启轮询，已用显式轮询版本修复并重新走通完整流程。
- 独立普通管理员真实 UI 登录后被超管停用，刷新回到登录页；测试账号已删除。大盘三次自动刷新保留地图节点，没有重新渐隐，地图逻辑回归通过。
- 迁移保留过期预览摘要及 `expired` 任务，旧非 UUID 标识仍可按权限查询；已补真实 HTTP 回归。
- pgBackRest 受控时钟实测：第 0、10、35 天各完整备份；第 35 天保留窗口起点仍依赖的第 0 天备份，第 45 天清理第 0 天备份并保留第 10、35 天链。只改隔离测试时钟，不改宿主机时间；不声称已有 30 天真实历史。
- Standards：初审 0 项硬性违规、1 项重复维护门禁的代码气味；抽取统一写入门禁后复审通过。
- Spec：初审 3 项实现问题（慢请求期间旧身份授权、并发作废锁升级死锁、最后一次重试的错误提示）；已修复并增加真实 HTTP／worker 回归，复审通过。轮询和迁移收尾另经只读复核，无新增阻断。
- 全仓 lint 有既有问题，未把它计作通过。没有进行容量或并发压测。

详细双轴报告见 [代码审查记录](review-report.md)。01–11 均已完成；生产已使用 PostgreSQL。

## 真实 OSS 与生产切换记录

- ECS `i-n4a7s4nbxdr2ad97qv8l`（武汉）绑定 `SentryBackupRole`。角色策略只允许 `floatnoise-wh/sentry/postgresql/` 对象操作与前缀列举；此前上海桶授权已从默认策略撤下。只读确认武汉桶 ACL 为 private。
- 主账户凭据只用于经用户明确授权的角色配置和桶信息检查，没有保存到代码、服务器或镜像。自动运行依赖 ECS 角色临时凭据。临时 ACR 登录配置、验收令牌和测试账号文件已清理。
- 首次真实连接发现镜像缺少 CA 证书，安装 `ca-certificates` 修复，保持 TLS 验证；随后发现 pgBackRest `info` 可在仓库 403 时退出 0，已检查 JSON 仓库状态并转换成非零失败，增加 CLI 合约回归。
- 成功演练前缀：`sentry/postgresql/rehearsal/sentry-oss-7fac57bcdf`。发布→真实 OSS 完整备份→作废→WAL 归档→从 OSS 恢复到 `2026-09-16 08:06:08.265177+00`；源为 withdrawn、恢复为 published，3 条结果、2 个试剂、1 个阳性一致，原账号字段和密码哈希恢复前核对、实际登录通过。
- OSS 无效令牌被明确拒绝；元数据网络不可达明确失败。完整小数据演练 106.62 秒，其中隔离恢复准备、下载回放、应用启动与核对 17.8 秒；镜像已预拉取，不涵盖重建 ECS、重新拉镜像、千万条数据或网络故障。此前本地缺 WAL 恢复失败与受控 30 天保留链证据仍有效。
- 所有本轮演练容器、卷与网络已核对清理。OSS 演练前缀保留作验收证据，后续仅按完整前缀清理，不执行桶级删除。
- 维护期间旧镜像重启与内部健康验证通过，随后停旧容器并保存冻结数据。原库与冻结副本 SHA-256 一致；保留副本设为只读。SQLite WAL 读取需要共享内存侧文件，因此迁移使用冻结副本的独立工作拷贝，不改原库；该工作拷贝用后已删除。
- 最终迁移：1 账号、2 会话、97 上传批次、5,152 试剂、41,216 结果，53 组统计核对一致。目标保持维护直到验收和首份正式备份完成。
- 真实域名浏览器验收使用独立 `sentry_acceptance` 数据库及完全虚构 Excel：上传→刷新找回→确认入库→历史→整批作废，大盘从 2/1 回到 0/0；普通管理员真实登录、越权拒绝、停用后退出以及超管退出通过。临时应用和验收库随后删除。原样表的远程上传被自动审批拒绝，未执行该传输；真实样表回归仅在本机运行。
- 原线上超管凭据登录迁移后后台成功；会话跨最终镜像重启保留，公开入口访问与退出通过。大盘三次自动刷新不重建地图、不触发整图渐隐。
- 正式部署：`/opt/sentry-pg`，首次完整备份 `20260916-082211F`，仓库 `sentry/postgresql/production/ecs-wuhan-20260916-main`。归档 check 成功，调度启动记录最近备份和磁盘余量正常。
- 北京时间 16:25:51 解除维护。真实浏览器移除验收请求头后，大盘、后台登录和健康接口均 HTTP 200，健康返回 `database: postgresql`。本机 curl 出现一次 TLS 连接异常，未将该命令计作通过，以独立浏览器及服务器检查为有效证据。
- 应用、worker、PostgreSQL 健康，PG 未发布宿主机端口。旧 SQLite 容器停止，数据及镜像保留；`writes-opened` 标记已写，禁止直接回退旧库丢弃后续写入。

最终生产镜像（固定摘要）：

- 应用：`sha256:fd3748f489144391e37f5661347197c4890a607d41f6ef59eae11b8e0742450d`。
- PostgreSQL／备份：`sha256:303d4fd6018223dc9e8678af733a094173eabe11f59df80940889ffafcebaa92`。

两者均位于用户指定 ACR `dypluto/sentry`。最终类型检查和生产构建通过，25 项全量测试通过。全仓 lint 的既有问题仍未宣称通过。
