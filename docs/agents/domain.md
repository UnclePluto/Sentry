# 领域文档约定

本项目采用单一业务上下文。

- 如存在根目录 `CONTEXT.md`，优先读取其中的领域术语。
- 如存在 `docs/adr/`，读取与当前变更相关的架构决策；发现冲突时明确指出，不静默覆盖。
- 上述文件不存在时正常继续，不将其缺失当作业务工作的阻塞条件。
- 规格使用用户已经明确的领域名称；本次重点区分检测批次号、试剂标识、病原体结果和业务上传批次，不能统称为同一种批次或数据行。
- 尚未实现的已确认规则以相应规格为依据；现有代码与旧 README 描述的是当前行为，不得据此覆盖用户新确认的规则。
